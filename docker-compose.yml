services:
  redis-stack:
    image: redis/redis-stack:latest
    container_name: atc-redis
    ports: ["6379:6379", "8001:8001"]   # 6379 数据端口；8001 RedisInsight 可视化
    volumes: ["atc-redis:/data"]

volumes:
  atc-redis:
